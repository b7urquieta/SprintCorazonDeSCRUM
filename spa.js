import React from 'react';
import ReactDOM from 'react-dom';
import ReservaCita from './react/ReservaCita';

ReactDOM.render(<ReservaCita />, document.getElementById('root'));

import React from 'react';
import ReactDOM from 'react-dom';
import ReservaCita from './react/HorarioTerapeutas';

ReactDOM.render(<ReservaCita />, document.getElementById('root'));